# Groq AI – get free key at https://console.groq.com/keys
GROQ_API_KEY=

# Twilio – phone and web voice
# 1) Account SID (starts with AC...) – Twilio Console dashboard
TWILIO_ACCOUNT_SID=

# 2) API Key – create in US1 region at Console → Account → API keys & tokens
#    SID (starts with SK...) → TWILIO_API_KEY
#    Secret (shown once at creation) → TWILIO_API_SECRET
TWILIO_API_KEY=
TWILIO_API_SECRET=

# 3) TwiML App SID (starts with AP...) – Console → Phone Numbers → Manage → TwiML Apps
#    Set its Voice URL to: https://your-ngrok-url.ngrok.io/voice
TWILIO_TWIML_APP_SID=

# 4) Voice region (default us1 – must match the region your API Key was created in)
TWILIO_VOICE_REGION=us1
